<?php
/**
 * Model generated by PHP-CLI
 */
namespace App\Models;

class <%- name %> {
    protected $table = '<%- tableName %>';
    protected $fillable = [<%- fillable.map(f => "'" + f + "'").join(', ') %>];

    public function all() {
        // Logic for fetching all
    }

    public function find($id) {
        // Logic for finding by id
    }
}
